# shiwake-ai セッション引き継ぎ 2026-05-17（後半）

## このセッションでやったこと

料金改定v3.2 の後始末2件を完了し、動作確認を実施した。

---

## 完了タスク

### 残課題A: WSアドオン関連の死にコード除去

- server.js / index.html のコード掃除
- `workspace_addon_subscriptions` テーブルと `users` の `has_workspace_option` / `workspace_addon_count` カラムを削除
- 設計書 `v3_2.md` を docs/ へ移動、引き継ぎmd追加
- **最終commit: b2aaef1**

### 残課題B: 旧edition機構の撤去

- `EDITION_FEATURES` / `featureEnabled` / `_userFeatures` 削除（休眠コード）
- `graduated_rookie_at`（ルーキー卒業ゲート）全廃
- `canUseAutoIntake` を `plan_key` 判定に変更（automation以上で自動取込可）
- 自動取込UI・WSモーダル自動ルール学習を `plan_key` 基準に
- `/api/user/graduation-status` エンドポイント撤去
- `users` の `edition` / `graduated_rookie_at` カラム削除
- **最終commit: 1d46592**

### 動作確認

料金改定v3.2 の通し確認（購入フロー／課金見込み／代理店トグル／機能マップ4タブ）と B 差分の確認を実施。料金改定の土台はバグなしを確認。

---

## 動作確認で判明した新規課題（記録済み）

| 課題 | 内容 | 状態 |
|------|------|------|
| **C** | 代理店トグル改善（Bronzeマージン率の画面表示、Silver/Gold含むランク別表示、ランク切替で価格反映） | B完了後に着手と約束済み |
| **D** | plan_key機能ゲーティングの未実装。basicでも上位機能が操作できる。3箇所（サイドバーのセクション/メニュー、アンロック機能画面のトグル、サイドバー経由の独立画面）に plan gate が無い。料金改定v3.2の積み残し。設計書§6.3が機能差の正本。 | 未着手 |
| **E** | ヘッダー余白不足のUI被り。右上カウンター（累計/マスタ）が下のUIに被る。全メニュー開始位置を下げてヘッダーに余白を与える方針。 | 未着手 |

---

## 次セッション冒頭の論点

ロードマップ上の次は「証憑回収機能」だが、課題Dが見つかったため、**D（機能ゲーティング実装）を証憑回収より先にやるかを最初に判断する**。

**理由:** Dが未対応だと、証憑回収をコンプリートに乗せてもbasicで機能が漏れる。

---

## 体制・進め方の前提

- Claude（指示出し）+ Claude Code（実装）の分担
- コードを書く作業は着手前に承認、先回り禁止、色分けマーク
- 見積もりは実績ベース、進捗確認は2段アコーディオン
