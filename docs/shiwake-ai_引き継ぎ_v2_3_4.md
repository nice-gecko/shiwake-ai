# shiwake-ai 引き継ぎメモ v2.3.4

> 作成日: 2026-05-12
> 前バージョン: v2.3.3(料金プラン28本 + Stripe 本番モード移行)
> 本バージョン: **v2.3.4(バグ報告システム + ホットフィックス群 + 対応漏れ全件処理)**

📌 **次セッション開始時の必須アクション**
1. `~/APP/shiwake-ai/CLAUDE.md` を最初に読むこと(2026-05-12 に CC 暴走パターンと再発防止ルールを追記)
2. 本メモを読む
3. それから着手判断

---

## 🎯 サマリー

朝9時開始、約9時間で v2.3.4 シリーズと運用基盤整備を完全終了。
設計書ベースだと「数週間相当」のスコープを1日で押し込んだ。

### 主要成果
- v2.3.4 バグ報告システム本体(FAB / モーダル / 異常検出 / スクショ / 楽観的UI / ファイル添付)
- v2.3.2 取りこぼし修正(`/api/analyze-chunk` の workspace_id 連携漏れ)
- ポーリング最適化(30秒 → 120秒、UI チラつき解消)
- 対応漏れ徹底検査 + 9件全件修正
- CLAUDE.md に CC 暴走パターン再発防止ルール追加

### 本日のコミット履歴

| カテゴリ | コミット | 内容 |
|---|---|---|
| v2.3.4 base | f5c60e9, 3ed6bad, c20c6f9 | バグ報告システム本体 |
| hotfix1 | 936dea6 | analyze-chunk workspace_id 漏れ修正 |
| hotfix2 | a4fdb3a | loadInboxTray ポーリング最適化 |
| hotfix3 | (Tabler CDN 修正) | FAB アイコン表示問題 |
| polish | 0ee261f | FAB トーン緩和 |
| security | 9b6d4f2 | 対応漏れ 9件全件修正 |
| attachments srv | 4ac8be6 | バグ報告添付ファイル機能のサーバー対応 |
| attachments fix | (日本語サニタイズ) | InvalidKey エラー解消 |
| ルール整備 | cc623cd | CLAUDE.md に CC 暴走パターン再発防止ルール追記 |


---

## 📅 作業ログ(時系列、約9時間)

### 09:00 — セッション開始、選択肢提示
- v2.3.3 完了状態から、4つの選択肢提示
- → A: v2.4.0 自動エクスポート選択

### 09:15 — v2.4.0 仕様詰め開始(骨格5論点)
- トリガー = 件数ベース、ユーザー設定可能
- アンロック条件 = 信頼度関係なし、最初から使えてデフォOFF、自分でアンロック
- 出力先 = メール / クラウド / ローカル DL の3種(WS設定で選択)
- 出力形式 = WS で記憶、初期値 = 弥生会計、変更したら次回引き継ぎ
- 失敗時通知 = メール + アプリ内 両方
- **語彙の訂正**: ルーキー/ジュニア/シニアの成長ストーリーは既に廃止、現在は信頼度メトリクスでアンロック制

### 09:45 — v2.3.4 バグ報告システム着手(優先度高で挿入)
- DSK 提案で v2.4.0 着手前に挿入
- 仕様: 右下フローティング + モーダル + console ログ自動同梱 + 異常検出
- スクショは html2canvas、異常検出は3段階(軽=バッジ/重=自動モーダル/致命=同上)

### 10:00 — Task 1〜3c 順次実装
- DB(bug_reports テーブル + Storage バケット)
- /api/bug-report エンドポイント(既存 sendAdminNotification 再利用)
- フロント基本機能、スクショ、異常検出フック

### 11:30 — 初回デプロイ、本番テスト → リグレッション発覚
- /api/analyze-chunk が 500 (workspaceId is required)
- 30秒ごとに自動モーダル発火 → うざい
- バグ報告自体が機能していない

### 11:45 — hotfix1 投入
- 原因: v2.3.2 ワークスペース機能対応の取りこぼし
- L1763, L1774, L1791, L1894 の4箇所修正(コミット 936dea6)

### 12:00 — hotfix2 投入(ポーリング最適化)
- graduation-status / inbox/settings を 10分キャッシュ、inbox のみ 120秒間隔
- 差分シグネチャで DOM 再構築スキップ(コミット a4fdb3a)

### 12:30 — FAB アイコン問題
- 🐛 絵文字が芋虫レンダリング → Tabler に変更
- CDN URL の 3.4.0 が cdnjs に存在せず 404
- 3.35.0 + ti-message に修正

### 13:30 — FAB トーン緩和
- 4案モック提示 → A 案(チャット吹き出し × slate-400)で確定
- コミット 0ee261f

### 14:00 — UI 改修(ステップバー組み込み)
- プログレスバー = ② 直下、仕訳カード = ③ 直下、CSV = ④ 直下
- 縦線4段貫通

### 14:30 — DSK 離席(2〜3h)、調査タスクを別 CC に投入
- 対応漏れ徹底検査タスク(コード変更禁止、レポートのみ)
- ⚠️ CC が「進めていいですか?」で止まって離席中に中断 → 再発防止ルール化

### 15:30 — 検出9件まとめて修正
- DSK 指示は「高3+中4」だったが、CC が低2件も勝手にコミット(暴走)
- 結果はレポート通りで妥当、許容
- workspace_id 漏れ3件 / レート制限 / サイズ制限 / admin認証 / localStorage uid 分離 / graduation-status 認証

### 16:30 — UX 改善(楽観的UI + ファイル添付)
- モーダル即閉じ + 右下トーストで状態表現
- 文言改善「不具合が起きている画面を一緒に送る(自動撮影)」
- 任意でファイル添付(画像最大3件、各5MB)

### 17:00 — attachments サーバー対応
- bug_reports に attachments JSONB 追加、Storage に保存

### 17:30 — InvalidKey エラー
- フロントは正しく送ってる、Storage が日本語ファイル名を拒否
- sanitizeFilename を ASCII セーフに強化

### 18:00 — CLAUDE.md に再発防止ルール追記(コミット cc623cd)

---

## 📸 現状スナップショット

### バージョン
- **本番**: v2.3.4(コミット cc623cd まで反映)
- **画面表示**: `v2.3.4 · Sonnet 4.6`

### 実装済み機能(v2.3.4 時点)
- AI仕訳(Claude Sonnet)
- 取引先マスタ学習
- 主要会計ソフト CSV 出力(freee / MF / 弥生 / 勘定奉行 / 汎用)
- 自動取り込み(メール / Dropbox / GDrive)
- スタッフ管理・インセンティブ
- 代理店制度(Bronze / Silver / Gold)
- ワークスペース機能(マルチテナント)
- 信頼度メトリクス(maturity_level)
- 料金プラン 28 本(Stripe 本番モード)
- **バグ報告システム(v2.3.4)** ← NEW
  - 右下フローティング FAB(グレー+チャット吹き出し)
  - 楽観的UI + トースト
  - 自動スクショ(html2canvas)
  - ファイル添付(最大3件、各5MB、画像のみ)
  - console ログ自動同梱(直近50件)
  - 異常検出フック + クールダウン60秒
  - SendGrid 経由でメール通知(support@shiwake-ai.com)
- レート制限 / サイズ制限(/api/bug-report 1分5件、ボディ25MB)
- /api/admin/cache-stats 認証保護
- localStorage uid 分離

### 既知の積み残し(優先度順)

#### 高
- なし

#### 中
- **v2.4.0 自動エクスポート機能**(本筋)
  - 仕様骨格確定済み(本メモ「09:15」参照)
  - 詳細仕様書はまだ書いていない
  - 実績見積 6〜10h、安全側で 7〜11h
- **未振り分けトレイ UI(§4.4)**
  - 実績見積 1.5〜2h
  - 兄(税理士)に渡す前にあると親切

#### 低
- **Stripe Coupon 本番作成** ← 本日完了(silver_discount 7.14% / gold_discount 14.28% / Forever)
- **Render Cron 月次バッチ有料化** ← スキップ判断、詳細は後述
- **本格営業準備**(LP、営業資料、第1号事例計画)


---

## 🎯 次セッションの選択肢

| 優先 | タスク | 実績見積 | 価値 |
|---|---|---|---|
| ★ | **v2.4.0 自動エクスポート詳細仕様書 → 実装** | 7〜11h | 本筋、Phase 2 |
| ★ | **未振り分けトレイ UI** | 1.5〜2h | 兄渡し前準備 |
| 営業 | LP / 営業資料 / 第1号事例計画 | 別軸 | 本格営業前 |

### v2.4.0 着手時の確認事項
- 仕様骨格は午前の対話で確定済み(本メモ「09:15」参照)
- 詳細仕様書を作る → Task 分解 → Claude Code に投入、の順
- 後方互換改修を含む可能性あり、+30〜60分の余裕を見る

---

## ⚠️ 重要な技術的注意点

### v2.3.2 取りこぼしパターン(再発防止)
- v2.3.2 ワークスペース機能追加時、複数箇所で workspace_id 連携漏れがあった
- 今日 `/api/analyze-chunk` で1件、検査レポートで3件発見・修正済み
- **新規エンドポイントを追加する際は、workspace_id の取り出し・受け渡しを必ずチェックリスト化**

### Claude Code の挙動(CLAUDE.md 参照)
- 「依頼範囲を超えて勝手にやる」傾向あり → CLAUDE.md に明文化済み
- 「進めていいですか?」で止まる頻度 → 離席時は冒頭で「確認なしで完走」と明記
- 1コミットの粒度 → 機能的に独立した修正は分離

### バグ報告システムの運用
- 通知メール宛先: `support@shiwake-ai.com`(Render 環境変数 ADMIN_EMAIL)
- スクショ保存先: Supabase Storage `bug-screenshots` バケット(private)
- DB: `bug_reports` テーブル(`attachments` JSONB あり)
- 検査タスク用のテストデータは Storage の `test-inspection-uid-20251112/` 配下に残ってる可能性 → 不要なら手動削除

---

## 🔍 補足: Render Cron について(2026-05-12 結論)

戦略メモ(2026-05-10)で「Render Cron 月次バッチ有料化」が積み残し項目に上がっていたが、本日改めて検討した結果:

**現状の月次処理はすべてリアルタイム計算で実装されている**
- 月次カウントリセット: ユーザー画面アクセス時に billing_period_end をチェックして自動リセット(server.js:1325 付近)
- 代理店ランク判定: 代理店ダッシュボード表示時に monthlyVolume を再計算(server.js:1438 付近)

つまり「決まった時刻に自動で動かすバッチ」は現時点で不要。

**Render の有料化($7/月)も現時点では先送り**
- Cron Job 機能は Render 有料プランから利用可能
- 必要になるのは「ユーザーが画面を開かなくても月初に強制リセット」のようなケース
- 代理店契約者0人 + ユーザーアクセスもまだ少ない現状では、リアルタイム計算で十分

**判断**: 本格営業に出る直前(または契約代理店が10件超え)のタイミングで再判断する。それまではスキップ。

---

## 📌 セッション終了時のチェックリスト(運用ルール)

1. ✅ 本メモを `~/APP/shiwake-ai/docs/` にコミット
2. ⏳ プロジェクトナレッジへの反映は手動(GitHub と自動同期されない、CLAUDE.md §プロジェクトナレッジ参照)
3. ⏳ 次セッション開始時、最新版を貼り付けるか手動差し替えする

---

**END OF 引き継ぎメモ v2.3.4**
